# Copy to server/config/local.toml overrides or a real .env for docker compose.
# Development normally needs none of this; cargo xtask zitadel-bootstrap writes
# server/config/local.toml directly. Kept here for parity with qa/production.

POSTLY_ENV=development
